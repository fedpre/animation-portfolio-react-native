import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SharedElementTransition = () => {
  return (
    <View>
      <Text>SharedElementTransition</Text>
    </View>
  );
};

export default SharedElementTransition;

const styles = StyleSheet.create({});
